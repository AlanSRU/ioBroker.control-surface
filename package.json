{
    "name": "iobroker.control-surface",
    "version": "0.0.0",
    "private": true,
    "description": "Semantic control and presentation layer for ioBroker — design stage, engine core in progress",
    "license": "MIT",
    "scripts": {
        "check": "tsc --noEmit",
        "test": "node --disable-warning=ExperimentalWarning --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --experimental-strip-types --test \"src/**/*.test.ts\"",
        "verify": "npm run check && npm test"
    },
    "devDependencies": {
        "@types/node": "22.10.2",
        "typescript": "~5.6.3"
    }
}
