# Auto-merges Dependabot PRs according to the rules in .github/auto-merge.yml.
#
# Replaces the scaffold's dependabot-auto-merge.yml, whose action is dead.
# Do not name that action even in a comment: repochecker's E8912 matches the
# string anywhere in the file, so a comment about it fails the check.

name: Auto-Merge Dependabot PRs

on:
  # Trigger when a PR is opened or updated.
  # WARNING: This runs in the PR base. DO NOT build untrusted code in this action.
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    # Only run if actor is dependabot
    if: github.actor == 'dependabot[bot]'

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Auto-merge Dependabot PRs
        uses: iobroker-bot-orga/action-automerge-dependabot@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
